# Пульт охраны банка
Этот репозиторий предназначен для внутреннего использования сотрудниками банка "Полтос". Доступ к БД только через учетную запись. Если Вам просто интересна реализация запросов, можете ознакомиться с кодом и свободно его использовать. 

Пульт охраны позволяет подключиться в БД, которая хранит информацию о визитах и карточках сотрудников, и получать из нее необходимую информацию.

### Как установить

Для работы приложения будет необходима установка `Python 3`. После чего нужно подгрузить необходимые пакеты для нормальной работы скрипта с помощью `pip`. Для этого введите в командной строке команду:
```
pip install -r requirements.txt
```
Также необходимо создать переменное окружение с название `.env`, которое содержит данные для доступа к нужной БД. Для успешного доступа необходимо указать следующие параметры:
- `DB_SECRET_KEY` - секретный ключ
- `DATABASE_URL` - данные для доступа к Вашей БД оформленные в виде ссылки по схеме: `postgresql://USER:PASSWORD@HOST:PORT/NAME`.
- `DB_DEBUG` - параметр, который отвечает будет ли сайт при ошибке отображать отладочную информацию, `True` - режим отладки, `False` - режим пользователя.
> **Примечание** - указанная схема релевантна для БД PosgreSQL, если Вы используете другие базы данных, тогда ознакомьтесь с документацией по пакету [dj-database-url](https://github.com/jazzband/dj-database-url?tab=readme-ov-file#url-schema) и выберите там схему по Ваш случай.

### Как пользоваться

С помощью следующей команды в командной строке указываем путь в дерикторию с репозиторием:
```
cd *путь к локальной директории*
```
После этого вводим следующую команду для запуска сервера:
```
python manage.py runserver *ip сервера*
```
> **Примечание** для запуска локального сервера используйте ip - `0.0.0.0:8000`

После этого вы сможете зайти на сайт по ссылке:
```
http://localhost:8000/
```

<img width="1338" height="444" alt="image" src="https://github.com/user-attachments/assets/6e14ba79-8e29-47dd-a193-2783234f2715" />

На главной странице отображается информация по всем активным пропускам. При нажатии вверху на кнопку "Список пользователей в хранилище" откроется список ещё не закрытых визитов:

<img width="1532" height="321" alt="image" src="https://github.com/user-attachments/assets/3135504c-14da-4f28-b026-1e30c5e9f53b" />

На этой странице по кнопке "Список активных карт доступа" можете вернуться на главную страницу с активными пропусками. На главной странице при нажатии на имя владельца интересующего пропуска, откроется страница со всеми визитами данного человека:

<img width="1271" height="494" alt="image" src="https://github.com/user-attachments/assets/67079729-5eea-4a07-b45b-3abbb9650e37" />

Чтобы выключить сервер необходимо в консоле нажать `Ctrl+C`.

### Цель проекта

Данный проект создан в учебных целях.

